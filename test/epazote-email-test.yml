config:
    smtp:
        headers:
            from: from@email
            to: ops@domain.tld
            subject: >
                [%s - %s], Service, Status

services:
    service X:
        test: pgrep nginx
        if_not:
            notify: test
